apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    name: user-service-svc
    app: ctc-rest-app
spec:
  type: LoadBalancer
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 31003
  selector:
    name: user-service-pod
    app: ctc-rest-app  
  
